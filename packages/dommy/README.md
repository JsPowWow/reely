# dommy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dommy` to build the library.

## Running unit tests

Run `nx test dommy` to execute the unit tests via [Vitest](https://vitest.dev/).
